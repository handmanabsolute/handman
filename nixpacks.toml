[phases.build]
cmds = ["composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader", "npm install", "npm run build"]

[phases.release]
cmds = ["php artisan config:clear", "php artisan migrate --force"]
